<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Harjoitustyo" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/primary_pink</item>
        <item name="colorOnPrimary">@color/primary_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_background</item>
        <item name="colorSecondaryVariant">@color/secondary_pink</item>
        <item name="colorOnSecondary">@color/secondary_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorButtonNormal">@color/white</item>
    </style>
    
    <style name="Theme.Harjoitustyo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <item name="colorAccent">@color/primary_pink</item>
    </style>
</resources>